MONGODB_URI=
RESEND_API_KEY=
RESEND_FROM=

# Backend URL for WebRTC signaling (Socket.io server)
# For local development: leave empty (uses window.location.origin)
# For production: set to your deployed backend URL (e.g., https://your-backend.onrender.com)
VITE_BACKEND_URL=
